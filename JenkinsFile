pipeline {
    
    agent any

    environment {
        CI = 'true'
    }

    stages {
        stage('Install') { 
            steps {
                bat 'npm install'
            }
        }

        stage('Build') { 
            steps {
                bat 'npm config set registry https://registry.npm.taobao.org'
                bat 'npm run-script build'
            }
        }

        stage('Deploy') {
            steps {
                bat 'xcopy "build" "D:/dev/jenkins/repositories/react-app"'
            }
        }
    }
}